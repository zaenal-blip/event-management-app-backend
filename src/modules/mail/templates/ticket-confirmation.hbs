<!DOCTYPE html>
<html>

<head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Your Ticket for {{eventTitle}}</title>
    <style>
        body {
            font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, 'Helvetica Neue', Arial, sans-serif;
            line-height: 1.6;
            color: #1a1a1a;
            background-color: #f5f5f5;
            margin: 0;
            padding: 20px;
        }

        .container {
            max-width: 600px;
            margin: 0 auto;
            background-color: #ffffff;
            border-radius: 12px;
            overflow: hidden;
            box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
        }

        .header {
            background: linear-gradient(135deg, #4f46e5 0%, #7c3aed 100%);
            color: white;
            padding: 40px 24px;
            text-align: center;
        }

        .header-icon {
            font-size: 48px;
            margin-bottom: 16px;
        }

        .header h1 {
            margin: 0;
            font-size: 24px;
            font-weight: 700;
        }

        .header p {
            margin: 8px 0 0;
            font-size: 16px;
            opacity: 0.9;
        }

        .content {
            padding: 32px 24px;
        }

        .greeting {
            font-size: 18px;
            font-weight: 600;
            margin-bottom: 16px;
            color: #1a1a1a;
        }

        .message {
            color: #4a4a4a;
            margin-bottom: 24px;
        }

        /* Order summary box */
        .order-summary {
            background-color: #f8fafc;
            border-radius: 8px;
            padding: 20px;
            margin-bottom: 24px;
        }

        .order-summary h3 {
            margin: 0 0 16px;
            font-size: 16px;
            font-weight: 600;
            color: #1a1a1a;
        }

        /* Ticket card */
        .ticket-card {
            border: 2px solid #e2e8f0;
            border-radius: 12px;
            overflow: hidden;
            margin-bottom: 20px;
        }

        .ticket-header {
            background: linear-gradient(135deg, #4f46e5 0%, #6366f1 100%);
            color: white;
            padding: 16px 20px;
        }

        .ticket-body {
            padding: 20px;
        }

        /* Dashed divider */
        .ticket-divider {
            border: none;
            border-top: 2px dashed #e2e8f0;
            margin: 16px 0;
        }

        /* Important notes */
        .notes-box {
            background-color: #fefce8;
            border: 1px solid #fde68a;
            border-radius: 8px;
            padding: 16px 20px;
            margin-top: 24px;
        }

        .notes-box h4 {
            margin: 0 0 8px;
            font-size: 14px;
            font-weight: 600;
            color: #92400e;
        }

        .notes-list {
            margin: 0;
            padding-left: 18px;
            color: #78350f;
            font-size: 13px;
        }

        .notes-list li {
            margin-bottom: 4px;
        }

        /* CTA button */
        .button-container {
            text-align: center;
            margin: 28px 0;
        }

        .cta-button {
            display: inline-block;
            background: linear-gradient(135deg, #4f46e5 0%, #7c3aed 100%);
            color: white !important;
            text-decoration: none;
            padding: 14px 32px;
            border-radius: 8px;
            font-weight: 600;
            font-size: 16px;
            box-shadow: 0 4px 12px rgba(79, 70, 229, 0.4);
        }

        .footer {
            background-color: #f8fafc;
            padding: 24px;
            text-align: center;
            border-top: 1px solid #e2e8f0;
        }

        .footer p {
            margin: 0 0 8px 0;
            font-size: 13px;
            color: #6b7280;
        }

        .footer .brand {
            font-weight: 600;
            color: #4f46e5;
        }

        .social-links {
            margin-top: 16px;
        }

        .social-links a {
            display: inline-block;
            margin: 0 8px;
            color: #6b7280;
            text-decoration: none;
            font-size: 13px;
        }
    </style>
</head>

<body>
    <div class="container">
        <!-- Header -->
        <div class="header">
            <div class="header-icon">🎟️</div>
            <h1>Your Tickets Are Ready!</h1>
            <p>{{eventTitle}}</p>
        </div>

        <div class="content">
            <p class="greeting">Hi {{customerName}}! 👋</p>
            <p class="message">
                Great news! Your transaction has been confirmed. Below are your e-tickets for
                <strong>{{eventTitle}}</strong>. Please present the QR code at the venue for check-in.
            </p>

            <!-- Order Summary using TABLE -->
            <div class="order-summary">
                <h3>📋 Order Summary</h3>
                <table width="100%" cellpadding="0" cellspacing="0" border="0"
                    style="border-collapse: collapse; font-size: 14px;">
                    <tr>
                        <td style="color: #6b7280; padding: 6px 0;">Order ID</td>
                        <td align="right" style="font-weight: 600; color: #1a1a1a; padding: 6px 0;">#{{orderId}}</td>
                    </tr>
                    <tr>
                        <td style="color: #6b7280; padding: 6px 0;">Event</td>
                        <td align="right" style="font-weight: 600; color: #1a1a1a; padding: 6px 0;">{{eventTitle}}</td>
                    </tr>
                    <tr>
                        <td style="color: #6b7280; padding: 6px 0;">Date</td>
                        <td align="right" style="font-weight: 600; color: #1a1a1a; padding: 6px 0;">{{eventDate}}</td>
                    </tr>
                    {{#if eventVenue}}
                    <tr>
                        <td style="color: #6b7280; padding: 6px 0;">Venue</td>
                        <td align="right" style="font-weight: 600; color: #1a1a1a; padding: 6px 0;">{{eventVenue}}</td>
                    </tr>
                    {{/if}}
                    <tr>
                        <td style="color: #6b7280; padding: 6px 0;">Ticket Type</td>
                        <td align="right" style="font-weight: 600; color: #1a1a1a; padding: 6px 0;">{{ticketTypeName}}
                        </td>
                    </tr>
                    <tr>
                        <td style="color: #6b7280; padding: 6px 0;">Quantity</td>
                        <td align="right" style="font-weight: 600; color: #1a1a1a; padding: 6px 0;">{{ticketQty}}</td>
                    </tr>
                </table>
            </div>

            <!-- Ticket Cards -->
            <h3 style="margin: 0 0 16px; font-size: 16px; font-weight: 600;">🎫 Your E-Tickets</h3>

            {{#each tickets}}
            <div class="ticket-card">
                <!-- Ticket header using TABLE -->
                <table width="100%" cellpadding="0" cellspacing="0" border="0"
                    style="background: linear-gradient(135deg, #4f46e5 0%, #6366f1 100%); border-collapse: collapse;">
                    <tr>
                        <td style="padding: 16px 20px; color: white;">
                            <div style="font-size: 12px; opacity: 0.85;">Order #{{../orderId}}</div>
                            <div style="font-size: 16px; font-weight: 700; margin-top: 4px;">{{../eventTitle}}</div>
                        </td>
                        <td align="right" style="padding: 16px 20px; color: white;">
                            <div style="font-size: 12px; opacity: 0.85;">Ticket</div>
                            <div style="font-size: 18px; font-weight: 700;">{{this.ticketNumber}}/{{../ticketQty}}</div>
                        </td>
                    </tr>
                </table>

                <div class="ticket-body">
                    <!-- Info grid using TABLE -->
                    <table width="100%" cellpadding="0" cellspacing="0" border="0"
                        style="border-collapse: collapse; margin-bottom: 8px;">
                        <tr>
                            <td width="50%" style="padding: 6px 0;" valign="top">
                                <div
                                    style="font-size: 11px; color: #6b7280; text-transform: uppercase; letter-spacing: 0.5px; margin-bottom: 4px;">
                                    Date</div>
                                <div style="font-size: 14px; font-weight: 600; color: #1a1a1a;">{{../eventDate}}</div>
                            </td>
                            <td width="50%" style="padding: 6px 0;" valign="top">
                                <div
                                    style="font-size: 11px; color: #6b7280; text-transform: uppercase; letter-spacing: 0.5px; margin-bottom: 4px;">
                                    Ticket Type</div>
                                <div style="font-size: 14px; font-weight: 600; color: #1a1a1a;">{{../ticketTypeName}}
                                </div>
                            </td>
                        </tr>
                        <tr>
                            <td width="50%" style="padding: 6px 0;" valign="top">
                                <div
                                    style="font-size: 11px; color: #6b7280; text-transform: uppercase; letter-spacing: 0.5px; margin-bottom: 4px;">
                                    Attendee</div>
                                <div style="font-size: 14px; font-weight: 600; color: #1a1a1a;">{{../customerName}}
                                </div>
                            </td>
                            {{#if ../eventVenue}}
                            <td width="50%" style="padding: 6px 0;" valign="top">
                                <div
                                    style="font-size: 11px; color: #6b7280; text-transform: uppercase; letter-spacing: 0.5px; margin-bottom: 4px;">
                                    Venue</div>
                                <div style="font-size: 14px; font-weight: 600; color: #1a1a1a;">{{../eventVenue}}</div>
                            </td>
                            {{/if}}
                        </tr>
                    </table>

                    <hr class="ticket-divider" />

                    <!-- QR Code using CID attachment -->
                    <div style="text-align: center; padding: 8px 0;">
                        <img src="cid:{{this.qrCid}}" alt="QR Code" width="200" height="200"
                            style="width: 200px; height: 200px; display: block; margin: 0 auto;" />
                        <p style="font-size: 12px; color: #94a3b8; margin-top: 8px;">Scan this QR code at the venue for
                            check-in</p>
                    </div>
                </div>
            </div>
            {{/each}}

            <!-- Important Notes -->
            <div class="notes-box">
                <h4>📌 Important Notes</h4>
                <ul class="notes-list">
                    <li>Each QR code can only be scanned once</li>
                    <li>Screenshot or save this email for quick access</li>
                    <li>You can also view and download your tickets from the payment page</li>
                    <li>Arrive early to avoid long queues at check-in</li>
                </ul>
            </div>

            <!-- CTA Button -->
            <div class="button-container">
                <a href="{{viewTicketsLink}}" class="cta-button">View My Tickets</a>
            </div>
        </div>

        <!-- Footer -->
        <div class="footer">
            <p>Enjoy the event! We hope you have an amazing experience.</p>
            <p>With love, <span class="brand">The Eventku Team</span> 💜</p>
            <div class="social-links">
                <a href="#">Instagram</a> •
                <a href="#">Twitter</a> •
                <a href="#">Facebook</a>
            </div>
        </div>
    </div>
</body>

</html>
